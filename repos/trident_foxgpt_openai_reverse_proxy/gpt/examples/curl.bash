curl https://api.hypere.app/engines/text-davinci-003/completions \
   -H 'Content-Type: application/json, Authorization: {get a free key in our discord server!}' \
   -d '{
      "prompt": "Roses are red, "
   }'